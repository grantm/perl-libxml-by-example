* validating XML ?
  - handling parse errors
* URI retrieval (e.g. DTD) and the XML catalog

* customise the sphinx theme for larger font
* add an index:
  - http://www.sphinx-doc.org/en/stable/markup/misc.html#index-generating-markup

* XPath sandbox
  - update the URL on submit (allows going back to previous queries)
  - allow selecting between different canned XML documents
  - add support for namespaces

Glossary Terms

* DOM
* element
* node
* Clarkian notation
* document fragment
